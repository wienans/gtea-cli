# gtea-cli
Gitea CLI compatible with Github CLI

## Binary builds

Install Bun, then run `npm run build:binaries` to cross-compile standalone CLI binaries for Linux, Windows, and macOS into `dist/bin/`.
